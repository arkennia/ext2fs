#include "rmdir.h"

int rmdir_local(const char *pathname)
{
        int ino = 0;
        int parent_ino = 0;
        MINODE *mip = NULL;
        char name[256];
}

void rm_child(MINODE *parent_mip, char *file_name)
{
        
}
